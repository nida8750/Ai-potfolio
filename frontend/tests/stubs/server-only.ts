/**
 * The real `server-only` package throws when imported outside a server
 * context. Tests exercise those modules directly in Node, so this stub is
 * mapped in via tsconfig.test.json and is never used by the app build.
 */
export {};
