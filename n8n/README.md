# Nida AI — n8n webhooks

Local webhook base: `http://localhost:5678/webhook`

| Product | URL |
| --- | --- |
| LeadFlow | `http://localhost:5678/webhook/nida-ai/leadflow` |
| MailPilot | `http://localhost:5678/webhook/nida-ai/mailpilot` |
| InvoiceFlow | `http://localhost:5678/webhook/nida-ai/invoiceflow` |
| SupportSync | `http://localhost:5678/webhook/nida-ai/supportsync` |
| ContentFlow | `http://localhost:5678/webhook/nida-ai/contentflow` |

Keep workflows inactive until credentials and a sample POST succeed.
Do not invent secrets or claim a workflow is live until it has been tested.
